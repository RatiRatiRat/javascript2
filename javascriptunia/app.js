const button = document.querySelector('button')






button.addEventListener('click', () => {
    const img = document.querySelector('.nadziralaaaa'); // Selects the first image element with the class 'nadziralaaaa'
    img.classList.toggle('active'); // Toggles the 'active' class on the selected image
});
